import Joi from '@hapi/joi';

export const joiValidator = (data, schema) => {
  

    let message;
    const validationOptions = {
      allowUnknown: true, 
      stripUnknown: true, 
      abortEarly: false 
    };
     
 
 

  
  
  };